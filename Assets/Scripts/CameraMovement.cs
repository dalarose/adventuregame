using UnityEngine;

public class CameraMovement : MonoBehaviour
{
    [SerializeField]
    private Camera mainCamera;

    void Update()
    {
        // var tempCamPos = mainCamera.transform.position;
        // tempCamPos.x += 0.01f;
        // mainCamera.transform.position = tempCamPos;
    }
}
